# infra/kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

networking:
  disableDefaultCNI: true
  kubeProxyMode: "none"

nodes:
- role: control-plane
  extraMounts:
    - hostPath: /proc
      containerPath: /procHost
- role: worker
  extraMounts:
    - hostPath: /proc
      containerPath: /procHost
- role: worker
  extraMounts:
    - hostPath: /proc
      containerPath: /procHost
- role: worker
  extraMounts:
    - hostPath: /proc
      containerPath: /procHost